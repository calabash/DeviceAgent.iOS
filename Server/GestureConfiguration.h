
#import "ActionConfiguration.h"

/** A very simple wrapper around json options passed to a Gesture. 
 Generally these just need to be validated and not processed in any other way.
 */

@interface GestureConfiguration : ActionConfiguration
@end
